/**
 * Hardcoded values for the widths of the timeline "slots".
 *
 * We use this instead of an actual grid because the grid work was getting clunky. It's easier to
 * set hard values for the widths.
 */

export const periodWidth = 64;
export const iconWidth = 80;
export const maxTextWidth = 500;
